__pycache__/
*.pyc
.venv/
*.db
*.db-journal
*.db-wal
*.db-shm
.DS_Store

# Secrets — never commit. Template is .env.example.
.env

# Per-machine MCP config — path differs per checkout location.
# Template is .mcp.example.json (see comment inside).
.mcp.json

# Claude Code per-project harness state.
.claude/
